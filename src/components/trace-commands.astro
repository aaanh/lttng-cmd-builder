---
// Trace Commands Display Component
---

<div class="mt-6">
  <div class="flex items-center justify-between mb-3">
    <h3 class="text-lg font-semibold text-gray-800">Generated Commands</h3>
    <div class="flex items-center space-x-2 text-sm text-gray-600">
      <span id="commandCount" class="bg-gray-100 px-2 py-1 rounded"
        >0 lines</span
      >
      <button
        onclick="clearCommands()"
        class="text-red-600 hover:text-red-800 transition-colors"
      >
        Clear
      </button>
    </div>
  </div>

  <div class="relative">
    <pre
      id="traceCommands"
      class="whitespace-pre-wrap bg-gray-900 text-green-400 p-6 rounded-lg border border-gray-700 font-mono text-sm leading-relaxed overflow-x-auto shadow-lg min-h-[200px] max-h-[600px] overflow-y-auto">
    </pre>

    <!-- Copy button overlay -->
    <button
      onclick="copyCommandsDirect()"
      class="absolute top-3 right-3 bg-gray-800 hover:bg-gray-700 text-gray-300 hover:text-white px-3 py-1 rounded text-sm transition-colors opacity-0 hover:opacity-100 focus:opacity-100"
      title="Copy commands"
    >
      📋 Copy
    </button>

    <!-- Empty state -->
    <div
      id="emptyState"
      class="absolute inset-0 flex items-center justify-center text-gray-500"
    >
      <div class="text-center">
        <div class="text-4xl mb-2">🔧</div>
        <p class="text-sm">
          Configure your tracing options above to generate commands
        </p>
      </div>
    </div>
  </div>

  <!-- Command info -->
  <div class="mt-3 text-xs text-gray-500 flex items-center space-x-4">
    <span
      >💡 Commands will be generated in real-time as you configure options</span
    >
    <span>⚡ Use the buttons above to copy or download the script</span>
  </div>
</div>
